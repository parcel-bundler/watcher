{
  "name": "@parcel/watcher",
  "main": "index.js",
  "repository" : {
    "type" : "git",
    "url": "https://github.com/devongovett/fschanges.git"
  },
  "scripts": {
    "install": "prebuild-install || node-gyp rebuild",
    "prebuild": "prebuild -t 3 -r napi --strip",
    "prebuild-upload": "prebuild --all -u <github-token>",
    "test": "mocha"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^1.6.2",
    "prebuild-install": "^5.2.5"
  },
  "devDependencies": {
    "fs-extra": "^7.0.1",
    "mocha": "^6.0.2",
    "prebuild": "^8.2.1"
  },
  "binary": {
    "napi_versions": [
      3
    ]
  }
}
